# ${{ values.name }}

${{ values.description }}

## Responsibilities

- Hold the declarative manifests for the target cluster
- Capture environment-specific deployment intent
- Act as the catalog entry point for GitOps ownership and operations

## Cluster

- `${{ values.clusterName }}`

## Environment

- `${{ values.environment }}`